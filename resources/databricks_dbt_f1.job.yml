resources:
  jobs:
    databricks_dbt_f1_job:
      name: databricks_dbt_f1_job

      trigger:
        # Run this job every day, exactly one day from the last run; see https://docs.databricks.com/api/workspace/jobs/create#trigger
        periodic:
          interval: 1
          unit: DAYS

      #email_notifications:
      #  on_failure:
      #    - your_email@example.com

      tasks:
        - task_key: land_f1_data
          environment_key: default
          spark_python_task:
            python_file: ../src/ingestion/land_f1db.py

        - task_key: dbt
          depends_on:
            - task_key: land_f1_data
          environment_key: default
          dbt_task:
            project_directory: ../
            # The default schema, catalog, etc. are defined in ../dbt_profiles/profiles.yml
            profiles_directory: dbt_profiles/
            commands:
              - 'dbt deps --target=${bundle.target}'
              - 'dbt run --target=${bundle.target}'

      environments:
        - environment_key: default
          spec:
            environment_version: "4"
            dependencies:
              - dbt-databricks>=1.8.0,<2.0.0
